name: flutter_application_2
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # External packages
  image_picker: ^1.0.7
  cupertino_icons: ^1.0.8
  http: ^1.1.0
  url_launcher: ^6.2.5
  
  # Firebase packages
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1

  # Supabase
  supabase_flutter: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1

flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/majayjay.jpg"

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/